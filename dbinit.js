/* import Medicament from './medicament.js'
import data from './file.js'

data.map((item,index)=>{
var medicament =  new Medicament({info : item})
medicament.depopulate({})
medicament.save(function(err, doc) {
  if (err) return console.error(err);
  console.log("Document inserted succussfully!");
}); 
})
var str = "Space age"
export default str

 */